<resources>

    <!-- Base application theme. -->
    <style name="Ara" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/accent_material_light_2</item>
        <item name="android:statusBarColor">@color/accent_material_light_2</item>
    </style>

    <style name="Ara.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Ara.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Ara.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
